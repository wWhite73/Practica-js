import { processData } from './processData';

const numberArray = [1, 2, -3, 4, 5];
const stringArray = ["apple", "banana", "cherry"];

console.log(processData(numberArray)); // Выведет: 9
console.log(processData(stringArray));  // Выведет: 3